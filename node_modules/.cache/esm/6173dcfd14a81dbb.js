let Logger,Server;_14a‍.w("./classes/Logger",[["default",["Logger"],function(v){Logger=v}]]);_14a‍.w("./classes/Server",[["default",["Server"],function(v){Server=v}]]);


/**
 * Define Application start
 * @returns {Promise<void>}
 */
const start = async () => {
  try {
    // Initialize the server
    await Server.init();
  } catch (err) {
    Logger.error(`Error inside app start: ${err.message}`);
    throw err;
    process.exit(1);
  }
};

// Start the server
start();
